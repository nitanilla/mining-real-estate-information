# real_estate_agency-C
.
This aplication lets the user to add/update/delete an offer. 
Filters the existing offers by a field(price, surface) and a value smaller than the given one.
It can sort the existing offers by a given field(type and price) both ascending and descending.
