
Sample Real Estate
------------------

It's a sample project for real estate management program.
It is not finished, and just the first sketch.

You can compile it with:

    clang --std=c++11 -lc++ *.cpp

or if you don't have `clang` or `libc++`:

    g++ --std=c++11 -lstdc++ *.cpp

