# Real-estate-extraction
- This repo contains the code and data for the real estate extraction model.
- Run scripts in the following order to train model: `bash combine_all_text.sh`, `bash train.sh $(version) $(batch_size) $(num_epoch)`
- The model will be saved in folder `log_dir/runs/$(version)`
